
// in : "This is an example"
// out: "sihT is na elpmaxe"  


// "a" + "b" = "ab"
// w[0] + w[1]
// var newW = w[i] + newW

// var myNames = ["Nicholas", "Andrew", "Maxwell", "Morgan"]
// var my4Names = myNames.join(" ") // "Nicholas Andrew Maxwell Morgan"
// console.log(my4Names); // "Nicholas Andrew Maxwell Morgan"

Concatonation

The concat() method joins two or more strings.

Join "sea" and "food":

seafood

<script>
let text1 = "sea";
let text2 = "food";
let result = text1.concat(text2);

document.getElementById("demo").innerHTML = result;
</script>


Pushing and popping

// var str = "we ran"
// var strSplit = str.split(" ")    
// var words = []

// words.push(strSplit[0])
// words.push(strSplit[1]) 
// console.log(words)


// words.pop()
 

var str ="we ran" 
var strSplit = str.split(" ") 
var words = ["we "," ran "] 

words.push(strSplit[0]) 
words.push(strSplit[1]) 

console.log(words.pop()) 
console.log(words.pop())


Traditional Loops

Backwards
for (var i = array.length-1; i >= 0; i--) { 
    console.log(array[i])
}

Forwards
for (var i = 0; i < array.length; i++) { 
    console.log(array[i])
}

Habits in need of Adjustment

1. Align code,
2. Write 1 comment per goal, 
3. Write 1 line of code,  
4. Select more sensible and straight forward variables,
5. test in debugger, 
6. Use all developer's tool controls when stepping thru code
7. start process all over 

 









